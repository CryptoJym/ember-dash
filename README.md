# Ember

The fox. The pit. How far.

Play: space / tap to jump. Shift / DASH when you have it.

```bash
npm run serve          # http://127.0.0.1:8765
npm run sync           # refresh Capacitor www + iOS public
```

iOS archive still needs a signed Apple Developer team on the MacBook. See `docs/TESTFLIGHT.md`.

Bundle ID: `com.utlyze.emberdash`
